let sketchArea = 16;
let color = black;
let opacity = 10%;


function divGrid (sketchArea) {
	let grid = document.div[#sketchOutline];
	for (i = 0; i < sketchArea; i++) {
		let row = document.createElement("div");
		row.className = "row";
		for (i=0; i< sketchArea; i++) {
			let column = document.createElement("div");
			column.className = "column";
		}
	}
}